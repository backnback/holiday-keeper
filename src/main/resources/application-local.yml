spring:
  datasource:
    url: jdbc:h2:mem:hk_db;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create


custom:
  poolSize: 30
